name: Build & Publish carton

on:
  push:
    tags:
      - 'v*'
      - 'beta*'
  workflow_dispatch:
    inputs:
      version:
        description: 'SemVer (e.g. 1.2.3 or 1.2.3-beta.1)'
        required: true
        type: string
      channel:
        description: 'Release train to publish'
        required: true
        type: choice
        default: release
        options:
          - release
          - beta
      tag:
        description: 'Optional tag to create (defaults to v<version>-<channel> for manual runs)'
        required: false
        type: string

permissions:
  contents: write

env:
  DOTNET_NOLOGO: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true
  NUGET_XMLDOC_MODE: skip
  APP_NAME: carton

jobs:
  metadata:
    name: Resolve metadata
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.meta.outputs.version }}
      channel: ${{ steps.meta.outputs.channel }}
      is_prerelease: ${{ steps.meta.outputs.is_prerelease }}
      tag: ${{ steps.meta.outputs.tag }}
      release_name: ${{ steps.meta.outputs.release_name }}
    steps:
      - name: Compute release metadata
        id: meta
        shell: bash
        run: |
          set -euo pipefail
          event="${{ github.event_name }}"
          channel="release"
          tag=""
          version=""
          ref_name="${{ github.ref_name }}"

          if [[ "$event" == "workflow_dispatch" ]]; then
            version="${{ inputs.version }}"
            channel="${{ inputs.channel }}"
            tag="${{ inputs.tag }}"
            if [[ -z "$tag" ]]; then
              tag="v${version}-${channel}"
            fi
          else
            tag="$ref_name"
            if [[ "$ref_name" == v* ]]; then
              version="${ref_name#v}"
            else
              version="$ref_name"
            fi

            shopt -s nocasematch
            if [[ "$ref_name" == beta* ]] || [[ "$ref_name" == *-beta* ]] || [[ "$ref_name" == *-rc* ]] || [[ "$ref_name" == *-preview* ]]; then
              channel="beta"
            fi
            shopt -u nocasematch
          fi

          if [[ -z "$version" ]]; then
            echo "Version could not be resolved. Provide inputs.version when running manually." >&2
            exit 1
          fi

          if [[ "$channel" != "beta" ]]; then
            channel="release"
          fi

          is_prerelease="false"
          shopt -s nocasematch
          if [[ "$channel" == "beta" ]] || [[ "$version" == *-beta* ]] || [[ "$version" == *-rc* ]] || [[ "$version" == *-preview* ]]; then
            is_prerelease="true"
          fi
          shopt -u nocasematch

          app_name="${APP_NAME:-carton}"
          release_name="${app_name} ${version}"

          {
            echo "version=${version}"
            echo "channel=${channel}"
            echo "is_prerelease=${is_prerelease}"
            echo "tag=${tag}"
            echo "release_name=${release_name}"
          } >> "$GITHUB_OUTPUT"

  build:
    name: Build ${{ matrix.rid }}
    needs: metadata
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            rid: win-x64
            mainExe: carton.exe
            icon: src/carton.GUI/Assets/carton_icon.ico
          - os: windows-latest
            rid: win-arm64
            mainExe: carton.exe
            icon: src/carton.GUI/Assets/carton_icon.ico
          - os: ubuntu-22.04
            rid: linux-x64
            mainExe: carton
            icon: src/carton.GUI/Assets/carton_icon.png
          - os: ubuntu-22.04
            rid: linux-arm64
            mainExe: carton
            icon: src/carton.GUI/Assets/carton_icon.png
    env:
      VERSION: ${{ needs.metadata.outputs.version }}
      CHANNEL: ${{ needs.metadata.outputs.channel }}
    steps:
      - uses: actions/checkout@v6

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Install native dependencies (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y clang lld binutils-aarch64-linux-gnu gcc-aarch64-linux-gnu libc6-dev-arm64-cross zlib1g-dev libkrb5-dev pkg-config

      - name: Install Velopack CLI (Linux/macOS)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          dotnet tool update --global vpk --version 0.0.1298
          echo "$HOME/.dotnet/tools" >> "$GITHUB_PATH"

      - name: Install NSIS (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          choco install nsis -y --no-progress

      - name: Publish (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          publish_dir_portable="$GITHUB_WORKSPACE/artifacts/publish/${{ matrix.rid }}-portable"
          publish_dir_appimage="$GITHUB_WORKSPACE/artifacts/publish/${{ matrix.rid }}-appimage"
          mkdir -p "$publish_dir_portable" "$publish_dir_appimage"
          bash scripts/test-publish-linux-aot.sh "${{ matrix.rid }}" Release "$publish_dir_portable"
          bash scripts/test-publish-linux-aot.sh "${{ matrix.rid }}" Release "$publish_dir_appimage" INSTALLER_BUILD

          echo "PUBLISH_DIR_PORTABLE=$publish_dir_portable" >> "$GITHUB_ENV"
          echo "PUBLISH_DIR_APPIMAGE=$publish_dir_appimage" >> "$GITHUB_ENV"

      - name: Publish (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $publishDirPortable = "$env:GITHUB_WORKSPACE\artifacts\publish\${{ matrix.rid }}-portable"
          $publishDirInstaller = "$env:GITHUB_WORKSPACE\artifacts\publish\${{ matrix.rid }}-installer"
          $kernelStageDir = Join-Path $env:RUNNER_TEMP "singbox-runtime-${{ matrix.rid }}"
          New-Item -ItemType Directory -Path $publishDirPortable -Force | Out-Null
          New-Item -ItemType Directory -Path $publishDirInstaller -Force | Out-Null
          if (Test-Path $kernelStageDir) {
            Remove-Item -Recurse -Force $kernelStageDir
          }
          New-Item -ItemType Directory -Path $kernelStageDir -Force | Out-Null
          dotnet publish src\carton.GUI\carton.GUI.csproj `
            -c Release `
            -r ${{ matrix.rid }} `
            -o $publishDirPortable `
            /p:PublishAot=true `
            /p:SelfContained=true `
            /p:StripSymbols=true `
            /p:DebugSymbols=false `
            /p:DebugType=None `
            /p:InvariantGlobalization=true `
            /p:IncludeNativeLibrariesForSelfExtract=true `
            /p:EnableCompressionInSingleFile=true
          & "$env:GITHUB_WORKSPACE\scripts\include-singbox-kernel.ps1" -Rid '${{ matrix.rid }}' -Destination $kernelStageDir
          Copy-Item -Path (Join-Path $kernelStageDir '*') -Destination $publishDirPortable -Recurse -Force
          dotnet publish src\carton.GUI\carton.GUI.csproj `
            -c Release `
            -r ${{ matrix.rid }} `
            -o $publishDirInstaller `
            /p:CartonBuildMacro=INSTALLER_BUILD `
            /p:PublishAot=true `
            /p:SelfContained=true `
            /p:StripSymbols=true `
            /p:DebugSymbols=false `
            /p:DebugType=None `
            /p:InvariantGlobalization=true `
            /p:IncludeNativeLibrariesForSelfExtract=true `
            /p:EnableCompressionInSingleFile=true
          Copy-Item -Path (Join-Path $kernelStageDir '*') -Destination $publishDirInstaller -Recurse -Force
          "PUBLISH_DIR_PORTABLE=$publishDirPortable" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "PUBLISH_DIR_INSTALLER=$publishDirInstaller" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Create portable archive (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          pack_dir="$GITHUB_WORKSPACE/artifacts/pack/${{ matrix.rid }}-$CHANNEL"
          mkdir -p "$pack_dir"
          find "$PUBLISH_DIR_PORTABLE" -name '*.pdb' -delete
          portable_stage="$RUNNER_TEMP/portable-stage-${{ matrix.rid }}"
          rm -rf "$portable_stage"
          mkdir -p "$portable_stage"
          cp -a "$PUBLISH_DIR_PORTABLE"/. "$portable_stage"/
          touch "$portable_stage/.carton_portable_data"
          portable_name="${APP_NAME}-${VERSION}-${{ matrix.rid }}-portable.tar.gz"
          tar -C "$portable_stage" -czf "$pack_dir/$portable_name" .
          echo "PACK_DIR=$pack_dir" >> "$GITHUB_ENV"

      - name: Create portable archive (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $packDir = "$env:GITHUB_WORKSPACE\artifacts\pack\${{ matrix.rid }}-$env:CHANNEL"
          New-Item -ItemType Directory -Path $packDir -Force | Out-Null
          Get-ChildItem -Path $env:PUBLISH_DIR_PORTABLE -Filter '*.pdb' -Recurse | Remove-Item -Force
          $portableStage = Join-Path $env:RUNNER_TEMP "portable-stage-${{ matrix.rid }}"
          if (Test-Path $portableStage) {
            Remove-Item -Recurse -Force $portableStage
          }
          New-Item -ItemType Directory -Path $portableStage -Force | Out-Null
          Copy-Item -Path (Join-Path $env:PUBLISH_DIR_PORTABLE '*') -Destination $portableStage -Recurse -Force
          New-Item -ItemType File -Path (Join-Path $portableStage '.carton_portable_data') -Force | Out-Null
          $portableName = "$env:APP_NAME-$env:VERSION-${{ matrix.rid }}-portable.zip"
          $portableItems = Get-ChildItem -Path $portableStage -Force | Select-Object -ExpandProperty FullName
          if (-not $portableItems) {
            Write-Error "Portable staging directory is empty: $portableStage"
          }
          Compress-Archive -LiteralPath $portableItems -DestinationPath "$packDir\$portableName" -Force
          "PACK_DIR=$packDir" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Package with Velopack (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          icon_path=$(realpath '${{ matrix.icon }}')
          package_channel='${{ matrix.rid }}-'$CHANNEL
          vpk pack \
            --packId "$APP_NAME" \
            --packVersion "$VERSION" \
            --channel "$package_channel" \
            --runtime ${{ matrix.rid }} \
            --packDir "$PUBLISH_DIR_APPIMAGE" \
            --mainExe '${{ matrix.mainExe }}' \
            --packTitle "$APP_NAME" \
            --icon "$icon_path" \
            --outputDir "$PACK_DIR"

      - name: Package with NSIS (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Get-ChildItem -Path $env:PUBLISH_DIR_INSTALLER -Filter '*.pdb' -Recurse | Remove-Item -Force
          & "$env:GITHUB_WORKSPACE\scripts\build-nsis-installer.ps1" `
            -AppName "$env:APP_NAME" `
            -AppId "unifan.$env:APP_NAME" `
            -Version "$env:VERSION" `
            -MainExe '${{ matrix.mainExe }}' `
            -PublishDir "$env:PUBLISH_DIR_INSTALLER" `
            -OutputDir "$env:PACK_DIR" `
            -OutputFileName "$env:APP_NAME-$env:VERSION-${{ matrix.rid }}-Setup.exe" `
            -AppDataDirName "Carton" `
            -Publisher "Unifan" `
            -ProductRegKey "Software\Unifan\Carton"

      - name: Upload artifacts
        uses: actions/upload-artifact@v6
        with:
          name: vpk-${{ matrix.rid }}-${{ env.CHANNEL }}
          path: ${{ env.PACK_DIR }}

  publish:
    name: Publish GitHub release
    runs-on: ubuntu-latest
    needs:
      - metadata
      - build
    if: ${{ needs.build.result == 'success' }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          sparse-checkout: |
            .github/release-template.md

      - uses: actions/download-artifact@v8
        with:
          path: dist

      - name: Prepare release assets
        shell: bash
        env:
          VERSION: ${{ needs.metadata.outputs.version }}
        run: |
          set -euo pipefail
          shopt -s nullglob

          app_name="${APP_NAME:-carton}"
          release_dir="$RUNNER_TEMP/release-assets"
          rm -rf "$release_dir"
          mkdir -p "$release_dir"

          for artifact in dist/vpk-*; do
            [[ -d "$artifact" ]] || continue
            artifact_name=$(basename "$artifact")
            rid=$(echo "$artifact_name" | sed 's/^vpk-//;s/-[^-]*$//')

            while IFS= read -r -d '' setup; do
              cp -f "$setup" "$release_dir/${app_name}-${VERSION}-${rid}-Setup.exe"
            done < <(find "$artifact" -maxdepth 1 -type f -iname '*setup.exe' -print0)

            while IFS= read -r -d '' appimage; do
              cp -f "$appimage" "$release_dir/${app_name}-${VERSION}-${rid}.AppImage"
            done < <(find "$artifact" -maxdepth 1 -type f -iname '*.AppImage' -print0)

            while IFS= read -r -d '' portable; do
              cp -f "$portable" "$release_dir/$(basename "$portable")"
            done < <(find "$artifact" -maxdepth 1 -type f \( -name '*-portable.zip' -o -name '*-portable.tar.gz' \) -print0)

            while IFS= read -r -d '' nupkg; do
              cp -f "$nupkg" "$release_dir/$(basename "$nupkg")"
            done < <(find "$artifact" -maxdepth 1 -type f -name '*.nupkg' -print0)

            while IFS= read -r -d '' releases_json; do
              cp -f "$releases_json" "$release_dir/$(basename "$releases_json")"
            done < <(find "$artifact" -maxdepth 1 -type f -name 'releases*.json' -print0)
          done

          if ! find "$release_dir" -maxdepth 1 -type f | grep -q .; then
            echo "No release assets were prepared." >&2
            exit 1
          fi

          echo "RELEASE_ASSETS_DIR=$release_dir" >> "$GITHUB_ENV"

      - name: Create or update GitHub release
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ needs.metadata.outputs.tag }}
          REPO: ${{ github.repository }}
          RELEASE_NAME: ${{ needs.metadata.outputs.release_name }}
          IS_PRERELEASE: ${{ needs.metadata.outputs.is_prerelease }}
          TARGET_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail

          args=("$TAG" "--repo" "$REPO" "--title" "$RELEASE_NAME")
          if [[ "$IS_PRERELEASE" == "true" ]]; then
            args+=("--prerelease")
          fi

          if gh release view "$TAG" --repo "$REPO" >/dev/null 2>&1; then
            gh release edit "${args[@]}"
          else
            gh release create "${args[@]}" --target "$TARGET_SHA" --notes ""
          fi

      - name: Clear existing release assets
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ needs.metadata.outputs.tag }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail

          gh api "repos/$REPO/releases/tags/$TAG" --jq '.assets[].id' | while read -r asset_id; do
            [[ -n "$asset_id" ]] || continue
            gh api "repos/$REPO/releases/assets/$asset_id" --method DELETE
          done

      - name: Upload release assets
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ needs.metadata.outputs.tag }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          shopt -s nullglob

          uploads=()
          for pattern in '*-Setup.exe' '*.AppImage' '*-portable.zip' '*-portable.tar.gz' '*.nupkg' 'releases*.json'; do
            for f in "$RELEASE_ASSETS_DIR"/$pattern; do
              [[ -f "$f" ]] || continue
              uploads+=("$f")
            done
          done

          if [[ ${#uploads[@]} -eq 0 ]]; then
            echo "No files to upload." >&2
            exit 1
          fi

          gh release upload "$TAG" "${uploads[@]}" --repo "$REPO"

      - name: Build commit changelog
        id: changelog
        uses: mikepenz/release-changelog-builder-action@v5
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          mode: COMMIT
          toTag: ${{ needs.metadata.outputs.tag }}
          failOnError: false

      - name: Update release notes
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ needs.metadata.outputs.tag }}
          REPO: ${{ github.repository }}
          VERSION: ${{ needs.metadata.outputs.version }}
          CHANNEL: ${{ needs.metadata.outputs.channel }}
          CHANGELOG: ${{ steps.changelog.outputs.changelog }}
        run: |
          set -euo pipefail

          app_name="${APP_NAME:-carton}"
          base_url="https://github.com/$REPO/releases/download/$TAG"
          notes_file="$RUNNER_TEMP/release-notes.md"
          template_file=".github/release-template.md"
          singbox_tag="$(gh api repos/SagerNet/sing-box/releases/latest --jq .tag_name 2>/dev/null || true)"
          if [[ -n "$singbox_tag" ]]; then
            singbox_version="${singbox_tag#v}"
          else
            singbox_version="unknown"
          fi

          sed \
            -e "s|__APP_NAME__|$app_name|g" \
            -e "s|__VERSION__|$VERSION|g" \
            -e "s|__TAG__|$TAG|g" \
            -e "s|__REPO__|$REPO|g" \
            -e "s|__BASE_URL__|$base_url|g" \
            -e "s|__CHANNEL__|$CHANNEL|g" \
            -e "s|__SINGBOX_VERSION__|$singbox_version|g" \
            "$template_file" > "$notes_file"

          if [[ -n "${CHANGELOG//[[:space:]]/}" ]]; then
            {
              echo ""
              echo "**Commit Logs**"
              echo ""
              printf '%s\n' "$CHANGELOG"
            } >> "$notes_file"
          fi

          gh release edit "$TAG" --repo "$REPO" --notes-file "$notes_file"
